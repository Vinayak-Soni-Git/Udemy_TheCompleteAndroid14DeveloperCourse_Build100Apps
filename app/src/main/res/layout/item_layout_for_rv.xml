<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/groceryImage"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/bread" />

    <TextView
        android:id="@+id/groceryName"
        android:layout_width="215dp"
        android:layout_height="57dp"
        android:layout_marginStart="28dp"
        android:text="@string/title"
        android:textSize="32sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/groceryImage"
        app:layout_constraintTop_toTopOf="@+id/groceryImage" />

    <TextView
        android:id="@+id/groceryDescription"
        android:layout_width="215dp"
        android:layout_height="43dp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginStart="148dp"
        android:text="@string/your_description_here"
        app:layout_constraintBottom_toBottomOf="@+id/groceryImage"
        app:layout_constraintStart_toStartOf="@+id/groceryImage" />
</androidx.constraintlayout.widget.ConstraintLayout>